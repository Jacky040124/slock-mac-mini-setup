# 客户端 Mac mini 部署 Brewfile
# 配套：客户端_Mac_mini_部署清单.md
# 用法：brew bundle install --file=./Brewfile

# Formulas
brew "git"
brew "node"
brew "uv"
brew "tailscale"   # CLI/daemon version — supports `tailscale up --ssh` (cask GUI version doesn't)

# Casks
cask "ghostty"
cask "google-chrome"
cask "claude-code"
cask "beeper"
cask "obsidian"
