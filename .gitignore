# Brew bundle lockfile — keeps Brewfile pinning out of the repo
Brewfile.lock.json

# Install logs generated by install.sh
*.log
install-*.log

# Env files (may contain Tailscale auth key etc) — NEVER commit
*.env
.env

# macOS
.DS_Store
